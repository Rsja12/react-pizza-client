import React, { Component } from 'react'
import { connect } from 'react-redux'

export class Form extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default Form
